/** @type {import('next').NextConfig} */
const nextConfig = {}

module.exports = {
    images: {
      domains: ['127.0.0.1', '52.56.212.148'],
    },
    ignoreDuringBuilds: true,
    // ... any other existing configurations
  };
